
name: Build APK

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          # نقوم بتثبيت الحزم الأساسية ونستخدم || true لتجاهل الحزم التي قد لا تكون موجودة في النسخة الحديثة
          sudo apt-get install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev || true

      - name: Install Buildozer and Cython
        run: |
          pip3 install --upgrade pip
          pip3 install buildozer cython

      - name: Build with Buildozer
        run: |
          # سنقوم بضبط إعدادات Buildozer ليتم البناء بدون تفاعل
          buildozer android debug

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: LexInvoicePro-APK
          path: bin/*.apk
